Place bundled @trezor/connect iframe/popup assets in this directory.

`connectSrc` is `./trezor/` with `transports: ["WebUsbTransport"]` only.
Never load https://connect.trezor.io.

Run `bash scripts/vendor-trezor-connect.sh` after installing `@trezor/connect`.
